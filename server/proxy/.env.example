# Copy to .env and fill with keys
GOOGLE_MAPS_API_KEY=
OPENWEATHER_API_KEY=
GEMINI_API_KEY=
# A simple secret that your frontend sends in 'x-proxy-key' header to authorize requests (pick a random string)
PROXY_CLIENT_KEY=
# CORS allowed origins (comma separated) e.g. http://localhost:5500
ALLOWED_ORIGINS=http://127.0.0.1:5500,http://localhost:5500
PORT=4000
